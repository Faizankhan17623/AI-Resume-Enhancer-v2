# dependencies
node_modules/

# secrets sir — API keys, JWT keys, Razorpay secrets live here, NEVER push this
.env
.env.*

# logs
*.log

# local-only debug/admin scripts sir — e.g. listUsers.js, never meant for the repo/prod
scripts/listUsers.js
