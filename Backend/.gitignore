# dependencies
node_modules/

# secrets sir — API keys, JWT keys, Razorpay secrets live here, NEVER push this
.env
.env.*

# logs
*.log

# local-only debug/admin scripts sir — one-off tools run by hand against prod/local DBs,
# never meant to be tracked or deployed (e.g. makeAdmin.js, listUsers.js)
scripts/
